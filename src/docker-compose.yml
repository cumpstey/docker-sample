version: "3"
services:
  api:
    build:
        context: "./DockerSample.Api/"
    container_name: api
    #env_file: ".env"
    ports:
    - "5001:80"
  api2:
    build:
        context: "./DockerSample.Api2/"
    container_name: api2
    #env_file: ".env"
    ports:
    - "5002:80"
